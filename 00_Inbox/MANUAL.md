# ArchiveFileManager 操作・仕様マニュアル

本書は、圧縮ファイル（ZIP / RAR / 7z 等）の整理・最適化および空フォルダ削除ツール **「ArchiveFileManager」** の取扱説明書です。  
画面（GUI）の配置に沿って上から順に読み進めることで、各機能の役割と操作手順をスムーズに確認できるように構成されています。また、**画面上からは直接見えない安全設計・内部仕様**（話数別整理時の元ファイル保護やゴミ箱移動など）についても詳しく記載しています。

---

## 目次

1. [概要と動作環境](#1-概要と動作環境)
2. [画面構成と基本操作フロー](#2-画面構成と基本操作フロー)
3. [機能詳細：書庫クリーン・変換 タブ](#3-機能詳細書庫クリーン変換-タブ)
   - [3-1. 対象フォルダ設定エリア](#3-1-対象フォルダ設定エリア)
   - [3-2. 抽出・処理条件エリア（入れ子解消・話数別整理など）](#3-2-抽出処理条件エリア入れ子解消話数別整理など)
   - [3-3. 出力設定エリア（形式変換・圧縮率）](#3-3-出力設定エリア形式変換圧縮率)
   - [3-4. 削除パターン設定エリア](#3-4-削除パターン設定エリア)
   - [3-5. スキャン（プレビュー）エリア](#3-5-スキャンプレビューエリア)
   - [3-6. 実行・進捗・ログエリア](#3-6-実行進捗ログエリア)
4. [機能詳細：空フォルダ削除 タブ](#4-機能詳細空フォルダ削除-タブ)
   - [4-1. 検索対象と条件指定](#4-1-検索対象と条件指定)
   - [4-2. 検索とフィルタリング](#4-2-検索とフィルタリング)
   - [4-3. 安全な削除実行](#4-3-安全な削除実行)
5. [【重要】画面上からは確認できない内部仕様・安全機能](#5-重要画面上からは確認できない内部仕様安全機能)
   - [5-1. 話数別整理における元ファイル保護（_Original 退避）](#5-1-話数別整理における元ファイル保護_original-退避)
   - [5-2. 複数フォルダ同時指定（セミコロン区切り）](#5-2-複数フォルダ同時指定セミコロン区切り)
   - [5-3. フォルダ履歴の自動永続化 (`dir_history.txt`)](#5-3-フォルダ履歴の自動永続化-dir_historytxt)
   - [5-4. 日本語ファイル名（CP932/Shift_JIS）の文字化け自動補正](#5-4-日本語ファイル名cp932shift_jisの文字化け自動補正)
   - [5-5. 空フォルダ削除の「ゴミ箱」仕様（ESET等のファイルロック対策）](#5-5-空フォルダ削除のゴミ箱仕様eset等のファイルロック対策)
6. [トラブルシューティングとログ確認](#6-トラブルシューティングとログ確認)

---

## 1. 概要と動作環境

### 主な用途
- 電子書籍（自炊コミック・雑誌等）やアーカイブファイルの内部整理
- 広告URL・宣伝テキスト・サムネイルキャッシュ等の不要ファイル一括削除
- 書庫を開いたときにフォルダが余分に重なっている「入れ子階層」の自動解除
- 複数話が別フォルダに分かれている書庫の一括連番リネーム・フラット化（Cover優先）
- 書庫フォーマットの一括統一（RAR/7z → ZIP 等）および誤った拡張子のバイナリ自動補正
- ファイル整理後に残った「空フォルダ」の安全な一括検出・ゴミ箱への移動

### 動作環境
- **対応OS**: Windows 10 / 11 (64bit)
- **外部依存**: WinRAR (`Rar.exe` / `UnRAR.exe`)
  - ※ZIPの処理はPython標準ライブラリで行われますが、RAR形式の解凍・圧縮や7z処理にはWinRARを利用します。通常、標準的なインストール先（`C:\Program Files\WinRAR` 等）にあれば自動検出されます。

---

## 2. 画面構成と基本操作フロー

アプリを起動すると、上部に2つのタブが用意されています：

| タブ名 | 用途 |
| :--- | :--- |
| **🧹 書庫クリーン・変換** | 圧縮ファイルの中身を検査・整理・再圧縮・形式変換するメイン画面 |
| **📁 空フォルダ削除** | 指定ディレクトリ以下の空フォルダを安全にゴミ箱へ送る画面 |

### 基本操作の3ステップ
1. **対象フォルダを指定する**（「参照...」ボタンまたは直接入力）
2. **「🔍 スキャン（プレビュー）」をクリックする**（処理対象が一覧表示されます）
3. **内容を確認し、「実行」をクリックする**（必要なものだけチェックを入れて処理開始）

---

## 3. 機能詳細：書庫クリーン・変換 タブ

画面上部から下部に向かって配置されている各エリアの役割と使い方です。

### 3-1. 対象フォルダ設定エリア

- **対象フォルダ（入力欄 & ドロップダウン）**:
  - 処理したいフォルダのパスを入力します。過去にスキャンしたパスはドロップダウンから履歴選択できます。
  - セミコロン（`;`）で区切ることで、**複数のフォルダを同時に指定可能**です（例: `D:\Manga;E:\Novel`）。
- **「参照...」ボタン**:
  - フォルダ選択ダイアログが開きます。
  - **既に入力欄にパスがある場合**、「新しく選択したフォルダを追加しますか？（「いいえ」を選ぶと上書きします）」という確認メッセージが表示されます。「はい」を選ぶと、自動的にセミコロン区切りで末尾に追加されます。
- **サブフォルダも再帰的に検索する**:
  - チェックを入れると、指定フォルダ内の下層サブフォルダに含まれる書庫もすべて検索します。
- **更新日時絞り込み**:
  - 「7日以内」「30日以内」「90日以内」「365日以内」「カスタム」から選択し、最近変更・追加された書庫のみを対象にできます。

---

### 3-2. 抽出・処理条件エリア（入れ子解消・話数別整理など）

書庫に対して「どのような最適化を行うか」を選択するエリアです。

```
[✓] 指定パターンの不要ファイルを削除する
[✓] 余分な入れ子を解消する
    └ [✓] 話数別フォルダ・ファイル名整理 (Cover優先/v001_001... 連番化してルートにフラット化)
[✓] 長すぎる名前を短縮する
[✓] 拡張子を自動補正する
[✓] タイムスタンプを維持する
```

#### ① 指定パターンの不要ファイルを削除する
- 書庫内に含まれる広告テキストやショートカット等を自動削除します（対象パターンは後述の「削除パターン設定」で自由に変更可能）。

#### ② 余分な入れ子を解消する（親オプション）
- 書庫を開いた際、ルート直下に単一のフォルダ（例: `ComicTitle/`）しかなく、その中に画像ファイルが入っているような「余分な1段」を自動で引き上げ、書庫直下に画像を並べます。

#### ③ └ 話数別フォルダ・ファイル名整理（子オプション）
- **「余分な入れ子を解消する」の追加オプション**です。親チェックを外すと連動してグレーアウト（無効化）されます。
- **機能概要**:
  単一の巻数書庫の中に「第1話」「第2話」などの複数サブフォルダが存在する場合、そのままフラット化するとファイル名（`001.jpg` 等）が衝突して上書き消失してしまいます。本オプションを有効にすると、以下の規則で安全に連番リネームしてルート直下にフラット化します：
  1. **Coverの優先リネーム**: `cover` を含むフォルダやファイルを最優先し、自然順ソートで **`Cover_001.jpg`**, **`Cover_002.jpg`**... にリネーム。
  2. **各話フォルダのリネーム**: それ以外のフォルダを名前順にソートし、`v001`, `v002`, `v003`... を割り当て。フォルダ内の各画像を **`v001_001.jpg`**, **`v001_002.jpg`**... と連番リネーム。
  3. **ルートへの集約**: サブフォルダをすべて解除し、全画像をZIP直下に綺麗に並べます。
- **重要（安全保護仕様）**:
  話数整理が行われたファイルは、万が一の誤認に備えて**変換後のファイルが本来の名前で残り、元の書庫は自動的に `_Original` が付与されて安全に退避保存**されます（元ファイルは絶対に削除されません。詳細は [第5章](#5-1-話数別整理における元ファイル保護_original-退避) 参照）。

#### ④ 長すぎる名前を短縮する
- Windowsのパス長制限（260文字上限等）に引っかかる恐れのあるファイル名（80文字超過）を検出し、ハッシュ値を付与して安全な長さに短縮します。

#### ⑤ 拡張子を自動補正する
- ファイルのバイナリヘッダー（マジックナンバー）を解析し、中身がRARなのに拡張子が `.zip` になっているようなファイルを検知して、正しい拡張子へリネームします。

#### ⑥ タイムスタンプを維持する
- 処理・再圧縮後も、元々の書庫が持っていた更新日時（タイムスタンプ）をそのまま引き継ぎます。

---

### 3-3. 出力設定エリア（形式変換・圧縮率）

- **出力フォーマット**:
  - `元の形式を維持`（ZIPはZIP、RARはRARのまま）
  - `ZIP` / `RAR` / `7z`（指定した形式へ一括変換）
- **圧縮レベル**:
  - `無圧縮`（Store: 画像コミック向け・展開が最も高速でCPU負荷ゼロ）
  - `最速` / `高速` / `標準` / `高圧縮` / `最高圧縮`
- **変換後に元ファイルを削除する**:
  - 書庫形式の変換（例: RAR → ZIP）に成功した際、古い元ファイルを自動削除するかどうかを設定します。
  - ※**話数別整理が適用されたファイルは、このチェックがONであっても元ファイルは削除されず保護されます**。

---

### 3-4. 削除パターン設定エリア

- **削除対象パターンの登録・管理**:
  - デフォルト登録例: `*.txt`, `*.url`, `*.lnk`, `*.net`, `Thumbs.db`, `desktop.ini`, `.DS_Store`, `.com`, `zhonyk.*`, `*.rar`
  - 削除したいファイル名パターン（ワイルドカード `*` 対応）を入力して `[ 追加 ]` できます。
  - リストから選択して `[ 削除 ]`、または初期状態に戻す `[ リセット ]` が可能です。
  - 設定内容はアプリ終了時に `clean_patterns.txt` に自動保存され、次回起動時も保持されます。

---

### 3-5. スキャン（プレビュー）エリア

- **「🔍 スキャン（プレビュー）」ボタン**:
  - 設定した条件に基づいて書庫をスキャンし、何らかの処理が必要な書庫のみを抽出してツリービューに一覧表示します。**この段階ではファイルは一切変更されません**。
- **一覧ツリービューの見方**:
  | 列名 | 内容 |
  | :--- | :--- |
  | **☑** | 実行対象の選択チェックボックス（クリックでON/OFF） |
  | **書庫名** | 対象のファイル名 |
  | **フルパス** | ファイルが保存されている絶対パス |
  | **クリーン削除** | 検出された不要ファイルの件数（例: `3件`） |
  | **階層/名前** | 検出された構造最適化の内容（`階層` / `話数整理` / `名前`） |
  | **変換** | 形式変換や拡張子補正の予定内容（例: `RAR->ZIP`, `拡張子修正`） |
  | **更新日時** | ファイルの最終更新日時 |
- **便利機能**:
  - **列ヘッダークリック**: 各列で昇順・降順ソートができます。
  - **行のダブルクリック**: 該当する書庫が保存されているフォルダをエクスプローラーで開き、対象ファイルを選択状態にします。
  - **「すべて選択」「すべて解除」**: 一括でチェックを切り替えられます。

---

### 3-6. 実行・進捗・ログエリア

- **「🧹 選択した書庫をクリーン・変換実行」ボタン**:
  - チェックが入っている書庫に対して、一括処理を開始します。
- **「⏹ 中断」ボタン**:
  - 処理を途中で安全に停止します（現在処理中の書庫が完了した時点で中断されます）。
- **プログレスバー & ログ画面**:
  - 全体の進捗率（％）と、現在処理中のファイル名・各ステップ（解凍、削除、話数整理、再圧縮）のログがリアルタイムに流れます。
  - 完了時には「処理完了件数」「削除ファイル総数」「話数整理ファイル総数」のサマリーダイアログが表示されます。

---

## 4. 機能詳細：空フォルダ削除 タブ

ファイル整理後に残った不要な空フォルダを、ディレクトリツリーから一括走査して安全に削除する機能です。

### 4-1. 検索対象と条件指定
- **対象フォルダ**: 書庫クリーンと同様に履歴選択・複数フォルダ（`;` 区切り）の指定が可能です。
- **更新日時絞り込み**: 最近空になったフォルダのみを対象にしたい場合に利用します。

### 4-2. 検索とフィルタリング
- **「🔍 空フォルダを検索」ボタン**:
  - ボトムアップ走査（一番深い階層から走査）を行い、ファイルもサブフォルダも含まない「完全に空のフォルダ」を検出します。
  - 指定したルートフォルダ自体は削除対象から自動で除外されます。
- **フォルダ名絞り込み入力欄**:
  - 検出件数が多い場合、キーワードを入力することで一覧をリアルタイムに絞り込み検索できます。

### 4-3. 安全な削除実行
- **「🗑️ チェックした空フォルダを削除」ボタン**:
  - チェックを入れたフォルダを一括削除します。
  - **ゴミ箱への安全移動**: Shift+Deleteの完全消去ではなく、Windowsの**「ゴミ箱」へ移動**されます。万が一誤って削除した場合でも、ゴミ箱からワンクリックで元の場所へ復元できます。

---

## 5. 【重要】画面上からは確認できない内部仕様・安全機能

ユーザーが安心して作業を行えるよう、プログラム内部には高度な安全装置と自動処理が組み込まれています。

### 5-1. 話数別整理における元ファイル保護（_Original 退避）

話数別フォルダ・ファイル名整理は書庫内の構造を大幅に変更するため、書庫内が想定外の特殊構造だった場合に元データが失われないよう、以下の特別ルールが適用されます：

1. **命名ルールの自動反転保護**:
   - **整理後の新しい書庫**：本来の書庫名（例: `Title_Vol01.zip`）でそのまま作成されます。
   - **元の書庫（変更前）**：ファイル末尾に **`_Original`** が付与され、自動的にリネーム退避されます（例: `Title_Vol01_Original.zip`）。
2. **「元ファイル削除」設定の強制無効化**:
   - 出力設定エリアで「変換後に元ファイルを削除する」にチェックが入っていても、**話数別整理が適用された書庫については元ファイル削除が無効化**されます。
   - 処理後、必ず `_Original` ファイルが同一フォルダに残ります。変換結果を確認し、問題がないことを確認した後に手動で削除してください。

---

### 5-2. 複数フォルダ同時指定（セミコロン区切り）

- 対象フォルダ入力欄にセミコロン（`;`）を挟むことで、複数のドライブや離れたディレクトリを一度に指定できます。
  ```
  D:\Comic\Action;E:\Comic\Comedy;F:\Download\Archives
  ```
- スキャン時はそれぞれのフォルダが順次走査され、結果が1つのプレビュー画面に統合されます。
- 「参照...」ボタンからフォルダを追加するだけで自動的にセミコロンで連結されるため、手動入力の手間もありません。

---

### 5-3. フォルダ履歴の自動永続化 (`dir_history.txt`)

- 過去にスキャンを実行したフォルダパスは、アプリと同じ階層にある `dir_history.txt` に自動保存されます。
- アプリを終了・再起動しても履歴が消えることはなく、いつでもドロップダウンから呼び出せます。

---

### 5-4. 日本語ファイル名（CP932/Shift_JIS）の文字化け自動補正

- 古いツールで作成されたZIPファイルや、UTF-8フラグが誤って設定された日本のZIPファイルでは、ファイル名に含まれる「表」「予」「能」などの文字（0x5C問題）によって階層パスが誤認識され、予期せぬフォルダ分裂が発生することがあります。
- ArchiveFileManagerでは、解凍および内容一覧取得時に**CP932（Shift_JIS）へのフォールバック変換を自動適用**するため、日本語ファイル名やフォルダ名が化けることなく、正しい階層構造のまま認識・処理されます。

---

### 5-5. 空フォルダ削除の「ゴミ箱」仕様（ESET等のファイルロック対策）

- セキュリティソフト（ESET、Windows Defender等）がバックグラウンドでフォルダを監視・スキャンしている最中にプログラムから強制削除（`os.rmdir` やコマンドによる完全消去）を試みると、「アクセスが拒否されました（Permission Error）」が発生することがあります。
- 本アプリの空フォルダ削除は、Windows Shell標準の**ゴミ箱移動API（Send2Trash）**を採用しています。セキュリティソフトによるファイルロックとの競合を防ぎつつ、万が一必要なフォルダが空判定されて消されてしまった場合でも、Windowsの「ゴミ箱」から即座に戻すことができます。

---

## 6. トラブルシューティングとログ確認

### Q1. スキャン結果に書庫が表示されない
- **原因**: すでに最適化済み（不要ファイルがなく、入れ子もなく、名前の長さや拡張子も正常）なファイルは一覧に表示されません。
- **確認**: 「指定日数以内に更新されたファイルのみ」のチェックが入っていないか、またはサブフォルダ検索がOFFになっていないか確認してください。

### Q2. 処理中に「解凍に失敗しました」または「再圧縮に失敗しました」と出る
- **原因**: 書庫ファイル自体が破損しているか、RAR/7z処理に必要なWinRARが見つからない可能性があります。
- **対処法**:
  1. WinRARがインストールされているか確認してください。
  2. 実行ファイル（`.exe`）と同じフォルダに出力される **`archive_cleaner.log`** をテキストエディタで開いてください。エラーの詳細な原因（破損ブロック、パーミッションエラー等）が記録されています。

### Q3. 話数別整理を実行したら同じ名前のファイルが2つできた？
- **仕様です**: 元のファイル名（例: `Book.zip`）が整理後のファイルとなり、元の状態のファイルが `Book_Original.zip` として退避されています。中身を確認して問題がなければ `_Original.zip` は手動で削除してください。

---
*マニュアル作成日: 2026年9月*  
*ArchiveFileManager Project*
